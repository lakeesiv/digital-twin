# Introduction

This section describes how the simulation backend should be implemented. The simulation backend is responsible for running the simulation and providing the results to the frontend.

The `Jobs` section describes how the simulation backend should handle jobs. The `Results` section describes how the simulation backend should handle results and how it should provide them to the frontend.
